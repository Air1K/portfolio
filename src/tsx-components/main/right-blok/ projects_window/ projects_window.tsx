import React from 'react';

const ProjectsWindow = ({nav}: any) => {
   return (
      <section ref={nav}>
         <div></div> {/* Прилипает */}
      </section>
   );
};

export default ProjectsWindow;